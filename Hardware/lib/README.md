# Project libraries

Add independently reusable project libraries here. Application managers belong
under src/. Pin external dependencies in platformio.ini; do not copy generated
libraries into this directory.
