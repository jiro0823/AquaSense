# 🚀 QUICK START - Backend Server

## Start the Server (3 seconds)

```bash
cd Backend
npm run dev
```

**Expected Output:**
```
✓ Server is running on http://localhost:5000
✓ Environment: development
✓ API Version: v1
✓ CORS Origins: http://localhost:3000
```

---

## Test It Works

### In Browser
- **Health Check**: http://localhost:5000/health
- **Welcome**: http://localhost:5000/

### With cURL
```bash
curl http://localhost:5000/health
```

### With PowerShell
```powershell
Invoke-WebRequest http://localhost:5000/health -UseBasicParsing
```

---

## 📦 What's Installed

**Production Dependencies:**
- ✅ Express.js - Web framework
- ✅ TypeScript - Type-safe JavaScript
- ✅ Helmet - Security headers
- ✅ CORS - Cross-origin protection
- ✅ Morgan - Request logging
- ✅ dotenv - Environment config

**Development Tools:**
- ✅ Nodemon - Auto-reload
- ✅ ts-node - Run TypeScript directly
- ✅ ESLint - Code quality
- ✅ Prettier - Code formatting

---

## 📁 Folder Structure

```
Backend/
├── src/
│   ├── config/          # Configuration
│   ├── controllers/     # Request handlers
│   ├── middleware/      # Express middleware
│   ├── routes/          # Route definitions
│   ├── services/        # Business logic
│   ├── types/           # TypeScript types
│   ├── utils/           # Helper utilities
│   └── server.ts        # Main entry point
├── dist/                # Compiled JavaScript
├── .env                 # Environment variables
├── package.json         # Dependencies
├── tsconfig.json        # TypeScript config
└── README.md           # Full documentation
```

---

## All Available Commands

| Command | Purpose |
|---------|---------|
| `npm run dev` | Start with hot-reload (development) |
| `npm start` | Start production server |
| `npm run build` | Compile TypeScript to JavaScript |
| `npm run build:watch` | Watch and compile on file changes |
| `npm run lint` | Check code quality |
| `npm run lint:fix` | Fix linting issues |
| `npm run type-check` | Check TypeScript types |

---

## 🔧 Common Adjustments

### Change Server Port
Edit `.env`:
```env
PORT=3000
```

### Allow Different Frontend
Edit `.env`:
```env
CORS_ORIGIN=http://localhost:3000,http://localhost:8080
```

### Environment for Production
Edit `.env`:
```env
NODE_ENV=production
LOG_LEVEL=warn
```

---

## 📡 API Response Format

**All responses follow this format:**

```json
{
  "success": true,
  "statusCode": 200,
  "message": "Operation successful",
  "data": {
    "key": "value"
  }
}
```

---

## 🛡️ Security Features

✅ CORS protection
✅ Security headers (Helmet)
✅ Request size limiting
✅ Environment variables
✅ Error handling
✅ Graceful shutdown
✅ Input validation ready
✅ Rate limiting ready

---

## ❌ Stop Development Server

Press `Ctrl+C` in the terminal

---

## 📚 Learn More

- Read `README.md` for detailed documentation
- Read `SETUP_SUMMARY.md` for complete setup overview
- Check `src/` folder structure for code organization

---

**Status**: ✅ Ready to code!
**Setup**: April 12, 2026
