/**
 * EXAMPLE: User Routes
 * Path: src/api/v1/routes/user.ts
 *
 * This is a template for creating new route files.
 * Routes define the endpoints and map them to controllers.
 */

import { Router } from 'express';
// import {
//   getAllUsers,
//   getUserById,
//   createUser,
//   updateUser,
//   deleteUser,
// } from '../controllers/userController';

const router = Router();

/**
 * User endpoints
 * Base path: /api/v1/users
 */

// router.get('/', getAllUsers);                    // GET /api/v1/users
// router.get('/:id', getUserById);                // GET /api/v1/users/:id
// router.post('/', createUser);                   // POST /api/v1/users
// router.put('/:id', updateUser);                 // PUT /api/v1/users/:id
// router.delete('/:id', deleteUser);              // DELETE /api/v1/users/:id

export default router;
